// Tipos Literais: Crie um tipo literal chamado TrafficLight com os valores "red", "yellow" e "green". Em seguida, crie uma variável chamada currentLight e atribua um valor válido.

type TrafficLight = "red" | "yellow" | "green";

let currentLight: TrafficLight = "red";

console.log(currentLight);
