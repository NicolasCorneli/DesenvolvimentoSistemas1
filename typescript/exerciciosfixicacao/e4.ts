// Função com Retorno: Crie uma função chamada multiply que recebe dois números como parâmetros e retorna o produto deles. Chame essa função e exiba o resultado.

function multiply(x: number, y: number): number {
    return x * y;
}

let result = multiply(2,5);
console.log(result);
